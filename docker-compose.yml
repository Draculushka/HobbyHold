version: "3"

services:

  web:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - db
    environment:
      - DATABASE_URL=postgresql://draculushka:242684865@db/hobbyheaven

  db:
    image: postgres:16
    environment:
      - POSTGRES_USER=draculushka
      - POSTGRES_PASSWORD=242684865
      - POSTGRES_DB=hobbyheaven
